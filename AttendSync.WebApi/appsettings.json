{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "AttendSync": "Debug"
    }
  },
  "AllowedHosts": "*",
  "Urls": "http://localhost:5100",

  // Optional: pre-seed device config so the service auto-connects on startup.
  // If omitted, call POST /api/device/load-com then POST /api/device/connect manually.
  "DeviceConfig": {
    "IpAddress":   "172.16.10.111",
    "Port":        4370,
    "DeviceId":    1,
    "TimeoutSecs": 30,
    "Password":    "",
    "Brand":       "ZKTeco"
  }
}
